{{ wordpress.log_d }}/*.log
{
    missingok
    notifempty
    postrotate
        {{ wordpress.log_postrotate_f }}
    endscript
    # Ensure postrotate only runs once for all logs
    sharedscripts
}
