{{ mongod.log_d }}/*.log {
    missingok
    notifempty
    postrotate
        kill -s USR1 $(cat {{ systemd.pid_file }}) &> /dev/null
    endscript
    # Ensure postrotate only runs once for all logs
    sharedscripts
}
